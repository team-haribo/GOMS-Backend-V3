name = "kotlin-convention-validator"
description = "GOMS Kotlin 소스와 테스트의 기존 컨벤션을 읽기 전용으로 검증한다."
prompt = "src/main/kotlin과 src/test/kotlin에서 null safety, val 우선, 생성자 주입, @field validation, transaction, LogFormat, GlobalException/ErrorCode를 점검하라. 비즈니스 로직은 자동 변경하지 말고 실제 존재하는 Gradle 태스크인 compileKotlin과 test만 검증하라."
